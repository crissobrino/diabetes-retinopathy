Edits by Maria — 13/04
=======================
Covers: 11_densenet121_finetuning_INVALID

- Replaced EfficientNet-B0 with DenseNet121 as the FT backbone. Stage 1 unfreezes denseblock4+norm5 (~2.16M params, vs 3.15M for EfficientNet's features[6-8]). Head: Dropout(0.5) -> Linear(1024,1).
- Stage 2 unfreezes denseblock3+transition3 too, backbone lr=3e-5, head lr=1e-4, 3-epoch linear warmup then CosineAnnealingLR(T_max=12). Same gate/revert logic as before (needs Stage1+TTA val AUC >= 0.752).
- CustomNetV2 unchanged from 10_bengraham_fixed_4pass_tta_INVALID.
- Reasoning for DenseNet121 over EfficientNet-B0: dense feature reuse regularizes better on 2000 images, fewer Stage-1 trainable params, and it's the standard backbone for medical-imaging transfer (CheXNet lineage).
- Result at the time: custom 0.786/0.787, ft 0.781/0.781 — logged as the best scores in both categories across all experiments so far.
- Flagged NOT VALID later (see notebooks/archive/logs/scores.txt): this run's numbers weren't reproducible, so it was dropped from the trajectory. 19_customnetv2_3seed_ensemble_final_custom and 20_3backbone_weighted_ensemble_final_ft superseded it legitimately.

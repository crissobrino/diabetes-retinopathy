Edits by Maria — 10/04
=======================
Covers: 07_cached_images_ablation (discarded), 08_randomresizedcrop_label_smoothing

- 07: cached CropByEye+Rescale+CenterCrop(224) to disk as .npy, 5x faster epochs (~160s -> ~28s).
- 07 discarded: caching bakes in CenterCrop, so RandomCrop(224) — the main spatial regularizer on a 2000-image set — never runs. AUC dropped ~0.03 on both tracks (custom 0.736/0.730, ft 0.742/0.728) vs 06. Don't cache unless RandomCrop is preserved (e.g. cache larger, crop at runtime).
- 08 motivation: EfficientNet Stage 1 showed a 0.178 train/val AUC gap at its best checkpoint (memorizing the 2000 images).
- 08 change: TVRandomResizedCrop(224, scale=0.65-1.0) replaces Rescale(256)+RandomCrop(224) for training — adds scale jitter on top of position jitter.
- 08 change: label smoothing eps=0.05 for EfficientNet only (CustomNetV2 wasn't overfitting, left unchanged).
- 08 change: CustomNetV2 CosineAnnealingLR T_max 50->25, so two LR cycles fit in the 50-epoch budget instead of one (early stopping was firing right at the LR trough of a single 50-epoch cycle).
- 08 not executed yet at time of writing; target ft > 0.780 public, custom > 0.770 public.
